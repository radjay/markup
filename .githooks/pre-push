#!/bin/sh
# Pre-push hook: verify the project builds without errors
echo "Running build check..."
npm run check > /dev/null 2>&1
if [ $? -ne 0 ]; then
  echo "Build check failed. Fix errors before pushing."
  exit 1
fi
echo "Build check passed."
