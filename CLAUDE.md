# Markup

Electron-based native markdown editor for reviewing AI-generated plans and documents.

## What it does

- Opens markdown files (typically generated by coding agents like Claude Code)
- Allows selecting sections and leaving inline comments
- Supports document-level comments (not tied to a specific section)
- Supports direct edits to the markdown content
- Saves the file with comments and changes so the coding agent receives feedback and can continue

## Tech Stack

- Electron (desktop app)
- TypeScript

## Architecture

- Main process: Electron shell, file I/O, window management
- Renderer process: Markdown rendering, editing UI, comment system
- Preload: Bridge between main and renderer

## Development

- `npm start` — run in development mode
- `npm run build` — build for production
