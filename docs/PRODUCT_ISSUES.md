OPEN:

COMPLETED:

- Move the "review/edit" toggle to be floating in the right top corner of the document panel, not in the tab bar.

- I opened a new markdown file from claude code, and it immediately showed "reload file" bar at the top. Might have been fluke or something might not be working right.

- The right sidebar should be closable and collapsed by default. We should persist state of this setting.

- Open comment UI in doc still scroll back into view when continuing to read the doc! It now lets you scroll further, but when you stop, it still jumps back up.
